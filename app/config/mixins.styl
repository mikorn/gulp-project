max_screen(breakpoint)
  @media only screen and (max-width: breakpoint - 1)
    {block}
min_screen(breakpoint)
  @media only screen and (min-width: breakpoint)
    {block}
font_size(x)
  font-size round(fontSize * x)
